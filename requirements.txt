# ============================================================
# Hawkeye × Qwen3-VL — Main Environment Requirements
# ============================================================
# Install with: pip install -r requirements.txt
#
# This file pins the Qwen3-VL-first environment.
# The legacy LLaVA/Video-LLaVA path is retained for baseline
# comparison only and is NOT required for the main pipeline.

# ── Core HuggingFace stack ───────────────────────────────────
transformers>=4.57.0,<4.60.0
tokenizers>=0.21.0,<0.22.0
accelerate>=0.34.0,<1.0.0
huggingface-hub>=0.34.0,<1.0.0
safetensors>=0.5.0
sentencepiece>=0.2.0

# ── Training stack ──────────────────────────────────────────
deepspeed==0.17.1
peft>=0.9.0
bitsandbytes>=0.45.0

# ── Qwen3-VL multimodal utilities ───────────────────────────
# qwen-vl-utils provides fast video frame sampling via decord.
qwen-vl-utils[decord]==0.0.14
decord>=0.6.0
av>=12.0.0

# ── Hawkeye scene-enhancement dependencies ──────────────────
# torch-geometric is required for SceneGraphTower (GTN layers).
torch-geometric>=2.6.0
# fairscale provides memory-efficient optimizers used by llava_trainer.
fairscale>=0.4.13

# ── Common runtime utilities ─────────────────────────────────
numpy>=1.26.0,<2.0.0
pandas>=2.2.0
pillow>=10.3.0
scipy>=1.12.0
scikit-learn>=1.4.0
tqdm>=4.66.0
opencv-python>=4.10.0
einops>=0.8.0
datasets>=3.0.0
tensorboardx>=2.6.2
packaging>=24.0
pyyaml>=6.0.1
requests>=2.32.0

# ── Optional: faster attention kernels ──────────────────────
# Uncomment after confirming the server CUDA toolkit version.
# flash-attn==2.7.4.post1
